# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-07-14

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->
- **Salary model (2026-06-03):** Salary tab uses simple monthly net-entry model. No hourly/monthly type distinction. Each month user enters the net payslip amount directly (stored in `salaries.base`). External bonuses/deductions stored in `additions`/`deductions` JSONB. `total = base + additions - deductions`. Employee profile has no salary reference field.
- **Manual over automatic for money-matching (2026-07-05):** User explicitly rejected automatic FIFO reconciliation for supplier checks vs. debts — he sometimes deliberately pays a newer month's invoices while intentionally leaving an older month open (a dispute or business reason), and does NOT want the system to silently close the older debt first. Any debt/payment matching logic must let the user pick which specific debt(s)/month(s) a payment settles, with the system only suggesting a default (editable), never deciding unilaterally.
- **Debt is "settled" at check hand-over, not at due date:** From this user's POV, once he signs and hands over a check to a supplier, that debt is closed immediately — regardless of the check's future due date. The due date only matters for cash-flow forecasting (when money actually leaves the bank), never for whether a debt is considered paid.
- **Wants bulk-entry tools for repetitive data:** e.g. generating a whole series of checks (sequential check numbers + dates) in one action instead of typing each one — look for opportunities to batch near-identical manual entry across the app.
- **Always respond in Hebrew, not just when asked** (updated 2026-07-14): user first asked mid-session ("בעברית בבקשה"), then later explicitly reiterated "שים לב לשאול ולהגיב רק בעברית" (pay attention to ask and respond only in Hebrew) — this is a standing default for this project, not a one-off. Ask clarifying questions (AskUserQuestion labels/descriptions too) in Hebrew as well, not just prose replies.
- **Dictates requirements in a stream, expects them folded into the current task** (2026-07-06): during a single implementation pass on supplier-tracking payments, the user kept sending follow-up asks mid-edit (multi-debt payment picker → payment-method selector → quick-add-supplier → collapse-by-default accordion → color swap on charge/credit → per-line notes → retroactively link an already-issued check to a debt → Excel import → styled print). None were meant to interrupt or replace the current step — each was additive scope for the same feature. Pattern: finish the in-flight edit to a non-broken state first, then absorb the new ask into the same pass rather than asking "should I stop and redo?".

## Key Learnings

- **Project:** autoline-app
- **Description:** This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).
- **Role check pattern:** `isAdmin = profile.role === 'admin' || profile.role === 'super_admin'`. Non-admin employees should NOT see: AlertsPanel, RemindersPanel (dashboard), movements tab in Tires. Employee view in /employees shows only a phone-book (no salary data).
- **Tires permissions:** `isAdmin` controls access to the movements tab. `viewOnly` = has `tires_view` module but not `tires` and not admin (read-only inventory). Employees with neither module see nothing.
- **Shared profile/tenant context (2026-06-16):** `lib/contexts/ProfileContext.tsx` (`ProfileProvider`/`useProfile()`) fetches session+profile+full tenant row ONCE per page load. It's wired into `AppShell` (wraps Header, Sidebar, and the page's children). Any component rendered as a descendant of `AppShell` should use `useProfile()` instead of querying `auth.getUser()`/`profiles`/`tenants` itself — every `*Client.tsx` component, Header, Sidebar, and DashboardStats/Charts already do this. `profile.tenant` holds the full tenants row (all columns) so consumers needing business info (name/phone/address/logo/license_number/tax_id for invoices etc.) read it off `profile.tenant` instead of querying tenants again.
- **AppShell/Provider tree gotcha:** `useProfile()` only works in components rendered as JSX *children* of `<AppShell>`. A component that wraps itself in `<AppShell>...</AppShell>` in its own return statement (rather than being passed as AppShell's children from its page.tsx) CANNOT call `useProfile()` at its own top level — the Provider is a descendant of that component, not an ancestor, so context resolves to the default (null/loading forever). Fix: split into an outer wrapper (does the `<AppShell>` wrapping, no hooks needed) and an inner component (calls `useProfile()`, rendered as AppShell's child). This bit `ExpensesClient.tsx` and `app/dashboard/page.tsx` — both fixed by extracting an inner content component.
- **Print date format (2026-07-13):** printed reports (supplier ledger, customer ledger, supplier check calendar) show dates as `DD/MM/YY`, not ISO or `toLocaleDateString`. Use the `fmtDMY(d: string | Date)` helper defined next to `fmt()` in `SupplierTrackingClient.tsx`/`CustomerTrackingClient.tsx` — it string-splits `YYYY-MM-DD` values directly (no `Date`/`toISOString` round-trip) to avoid the UTC-shift off-by-one from bug-008. If a new printed view is added, reuse/copy this helper rather than `toLocaleDateString('he-IL')` (which gives `DD.MM.YYYY`, not the wanted format).
- **Netfree-evasion proxy on every Supabase call (found 2026-07-14):** `lib/supabase/client.ts`'s `createClient()` passes a custom `proxyFetch` to `createBrowserClient()` — every request whose URL starts with the real Supabase URL gets rewritten to `${NEXT_PUBLIC_APP_URL}/api/store?p=<base64(path+query)>` instead (comment: "hides the real Supabase path... so Netfree never sees supabase.co"). This means the browser never talks to `*.supabase.co` directly for REST/session calls; it all funnels through the `/api/store` Next.js API route. Two consequences to remember: (1) `tests/fixtures/mock.ts`'s `mockSupabase()` only does `page.route(SB_URL...)` — it does **not** intercept `/api/store`, so its empty-table stubs may silently not apply to some/most client calls; if the Playwright suite's data-dependent tests are flaky in ways that don't correlate with any real code change, suspect this mismatch before assuming cold-dev-server-compile timing. (2) Synthetic/scripted auth (e.g., constructing a session via `supabase.auth.admin.generateLink()` + injecting the resulting hash tokens into `/auth/callback` from a standalone script, bypassing the real `/login` form) does **not** reliably reproduce a real logged-in session in this app — confirmed the page renders (proxy.ts's server-side gate passes) but `useProfile()`/data fetches come back empty client-side, most likely because the proxy/session-cookie plumbing this synthetic path skips doesn't get set up the same way a real `/login` flow does. For any future need to browser-verify authenticated behavior, drive the real `/login` form with valid credentials rather than trying to shortcut auth.

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-06-23] Every page that renders a `*Client.tsx` component using `useProfile()` MUST wrap it in `<AppShell>...</AppShell>`. If AppShell is missing, `ProfileProvider` is absent, `useProfile()` returns null, and data never loads. Pattern: `return <AppShell><XxxClient /></AppShell>`. Scan page was missing this.
- [2026-07-06] After a long multi-round session (context compaction likely occurred), the in-context todo list can go stale relative to the actual file state — nearly told the user several features were "half-connected"/broken based on a remembered pending-todo list, when the file (and a clean `tsc --noEmit`) showed everything was already fully wired (modals rendered, props passed, Excel import + print already implemented). Before flagging incompleteness to the user, always verify against the actual file content / a fresh typecheck first, not against recollection of the task list.
- [2026-07-13] In a multi-step wizard, don't compute/commit derived state (like resolving an item's ok/fail status from its inputs) inside only ONE navigation handler (e.g. just the "next" button). Any other way to leave that step — back button, jump-to-step pills, "skip to summary" — must go through the same commit logic, or state silently never gets set for users who navigate any other way. Bit `InspectionChecklistModal.tsx`'s goNext/goPrev/jumpTo (bug-006): only goNext resolved status, so systems reviewed via the pills printed as blank rows. Fix pattern: extract a single `commitCurrentStep()` and call it at the top of every step-changing function, including `setStep(...)` call sites that aren't behind a named handler.
- [2026-07-07] User asked for a "3 cards per row" grid on the suppliers cards; I applied it to `DebtsClient.tsx`'s reduced suppliers-tab summary (a secondary view) instead of `SuppliersClient.tsx` (the actual sidebar page "ספקים / נותני שירות", the one the user actually opens day-to-day). ~~Three similarly-named surfaces exist for suppliers (`/suppliers`, `/debts` suppliers tab, `/supplier-tracking`)~~ **[SUPERSEDED 2026-07-14]** — `/supplier-tracking` no longer exists; it's now the "מעקב" sub-tab of `/suppliers` (see the 2026-07-14 merge entry in Decision Log). `/debts` also no longer has a suppliers tab at all. There is now only ONE surface for suppliers: `/suppliers` (with 2 sub-tabs). Same collapse happened for customers: `/customer-tracking` → `/customers`'s "מעקב" sub-tab.
- [2026-07-14] Same silent-rpc-error shape as bug-005, this time in `supabase/rpc()` calls instead of a `select()`: `decrement_tire_qty`/`decrement_product_qty` (migration 053_yard_rpcs.sql) wrote `UPDATE tires/products SET quantity = ...` but the real column is `qty` (per `003_all_tables.sql`) — the UPDATE errored every single time a yard session was archived, and since `app/api/yard/sessions/[id]/route.ts` never checked the `{error}` from `await supabase.rpc(...)`, stock silently never decremented, for the entire lifetime of the feature. **Do-Not-Repeat:** any `supabase.rpc(...)` call whose only purpose is a side effect (not reading data back) still needs its `error` checked/logged — an unchecked rpc() error is exactly as dangerous as an unchecked `select()` error (bug-005), just easier to miss because there's no `data` variable to notice is empty. Fixed via new migration `071_fix_yard_decrement_qty_column.sql` (CREATE OR REPLACE, not editing the old migration file — matches this project's convention of layering fixes as new migrations since old ones may already be applied to the live DB) plus added `console.error` on both rpc() call sites.
- [2026-07-14] `app/api/dev-login/route.ts` (dev-only auto-login using `DEV_EMAIL`/`DEV_PASSWORD` from `.env.local`) returned 401 "Invalid login credentials" when tried for browser-based verification — the stored password no longer matches Supabase. Couldn't get past this to click through an authenticated `/checks` page in-browser this session; fell back to `tsc --noEmit` + confirming the dev server compiles/serves the route with HTTP 200 unauthenticated (page shell renders, data-loading correctly no-ops without a `tenantId`). If asked to browser-verify again, check with the user for current credentials first rather than assuming `.env.local`'s are live.
- [2026-07-14] Both `CustomerTrackingClient.tsx` and `SupplierTrackingClient.tsx`'s ledger print (the shared `#print-area` + `body * { visibility: hidden }` + `position:absolute` technique) always produced one extra blank page. Cause: `visibility:hidden` doesn't collapse layout — `AppShell.tsx`'s `<main>` has an explicit `height: calc(100vh - var(--header-h))` + `overflowY:auto`, so that box's ~one-viewport height still occupies flow space during print even though invisible, and the browser paginates an extra blank page for it (Header/Sidebar are `position:fixed`, so they're not the cause). Fixed by adding `main { height: auto !important; overflow: visible !important; }` inside both files' `@media print` blocks (bug-013). **Any future print-area added via this same visibility-hack pattern needs this same `main` override** — or better, follow `InspectionChecklistModal.tsx::printChecklist()`'s alternative technique (opens a blank `window.open('', '_blank')` and writes a fully standalone HTML doc), which sidesteps the whole-app-layout-height problem entirely and has no blank-page issue.
- [2026-07-14] `CustomerTrackingClient.tsx`'s printed ledger (`printMode==='ledger'`) had a "יתרה בפועל" running-balance column that never netted `d.paid` against charges — only `openingForReport` (the carry-in from before the printed range) used `bal(d)`, which does net paid. So a customer's on-screen balance correctly reflected a payment (bal() everywhere on-screen already nets paid) but the *printed* running total for the in-range rows did not, making a payment look like it "didn't reduce the debt" specifically in the print output. Fixed by subtracting `d.paid` once per debt (on its last invoice-line row, charge-direction only) into the same `running` accumulator. Note: `SupplierTrackingClient.tsx`'s equivalent printed ledger deliberately does NOT net paid — that was an explicit user decision for suppliers (gross historical ledger, see the 2026-07-08 opening-balance entry below) — don't port this fix over there without asking first, the two print ledgers are now intentionally different in this one respect.
- [2026-07-14] Found while merging `/suppliers`+`/supplier-tracking` and `/customers`+`/customer-tracking` (see Decision Log): the very common `const tid = await resolveTenant(); if (!tid) return; setLoading(true); ...; setLoading(false)` pattern (used almost everywhere `useProfile()`-derived `tenantId` gates a fetch) leaves `loading` stuck at its initial `true` FOREVER if `resolveTenant()` returns null and profile-resolution never subsequently succeeds — there's no fallback path to flip `loading` false. In real usage this self-heals once `ProfileContext.load()` resolves (the `useCallback` dependency chain reruns the effect), so it went unnoticed for a long time. It became a real, reproducible bug once `/suppliers`/`/customers` joined the Playwright nav-test suite (`tests/navigation.spec.ts`) — under the test's mocked auth, `sb.auth.getUser()` resolves to `user: null` (a mock limitation, `ProfileContext.loading` still correctly flips to `false`), and pages using the bare `if (!tid) return` pattern hang on "טוען..." forever while `ExpensesClient.tsx` (which doesn't early-return the same way) renders fine. **Fix pattern**: destructure `loading: profileLoading` from `useProfile()` too, and when `resolveTenant()` fails, only keep spinning while `profileLoading` is still `true` — once the context has genuinely given up (`profileLoading === false && !profile`), call `setLoading(false)` so the page shows an empty shell instead of hanging. Applied to `SuppliersClient.tsx`, `CustomersClient.tsx`, `DebtsClient.tsx` shells this session. **Any other `*Client.tsx` added to the Playwright nav-test suite in the future should get this same fix if it uses the bare pattern** — check `resolveTenant`/`if (!tid) return` before assuming a "stuck on טוען..." test failure is unrelated flakiness.
- [2026-07-22] `SidebarLayoutEditor.tsx`'s open-effect seeded its editing state from the `defaultSections` prop, but `Sidebar.tsx` passed the *hardcoded* `SECTIONS` constant as that prop (not `activeSections`, the real current/saved layout) — so every reopen of "✦ ערוך סידור תפריט" silently discarded the previous save and showed the original default order, even though the save itself worked (the sidebar rendered the saved layout fine; only the editor's own next-open was wrong). Same "state doesn't cross a boundary" shape as bug-009/bug-010. Fix: added a separate `currentSections` prop for seeding, keeping `defaultSections` reserved for the reset button's true defaults. **Do-Not-Repeat:** any modal/editor with both a "current/saved" value and a "true default" value needs two distinct props — reusing one prop-name for both purposes (as if 'default' meant 'initial value') is exactly this bug. Check both call sites (what's passed in) and internal usage (what it seeds vs. what reset uses) whenever a settings/preferences editor "doesn't remember" what was saved.
- [2026-07-14] Mid-session, discovered another concurrent Claude Code session (which the user had running in parallel, working on an unrelated checks-journal feature) had a `git push` fire this session's auto version-bump **pre-push hook** while my own `git mv` (renaming `SuppliersClient.tsx`→`SupplierDetailsTab.tsx` etc., mid-refactor, before the follow-up edits landed) was sitting staged in the index — the hook's commit swept up my incomplete rename too, under an unrelated "chore: bump version" message, and it got pushed to `origin/main` before I noticed. User confirmed this was a real concurrent process (now stopped) and chose to leave the messy history commit as-is rather than rewrite it. **Do-Not-Repeat / awareness**: mid-task file content or git state can shift under you in this repo if the user has another session/process running — if a file's content doesn't match what you just read moments ago (e.g. a variable renamed that you didn't touch), stop and ask before continuing rather than assuming you misread it. See `[[feedback_prepush_hook]]` memory — user already wants this pre-push hook removed "next time it's relevant"; this incident is exactly that trigger.

## Key Learnings

- **Inspection checklist skeleton_only (2026-06-17):** skeleton_only flag stored in `findings` JSON as `{ skeleton_only: true, items: [...], notes: '' }`. No DB column needed. SKELETON_SYSTEM_INDICES = Set([16, 17]) — שלדת מרכב, מרכב (פחחות). Status 'na' added to ChecklistItem for non-skeleton systems. parseFindings() auto-applies 'na' when skeleton_only=true.

## Decision Log

- **[2026-07-14] Checks journal follow-up refinements, same session as the page below:** (1) `ChecksJournalClient.tsx`'s month blocks default to **collapsed**, not expanded — renamed `collapsedMonths`→`expandedMonths` (membership = user opened it), same inverted-default pattern already used for `SupplierTrackingClient.tsx`'s `expandedMonthKeys` (see the 2026-07-08 entry below) — reuse this pattern for any future per-month/per-group collapse state in this app, don't default to "membership = collapsed" since that requires seeding against a dynamic key set. (2) The checks-journal print output was flat (one table over all filtered rows); restructured to group by month with a per-month subtotal header, matching the on-screen layout — the bug-013 blank-extra-page fix (`main { height: auto !important; overflow: visible !important; }`) was already present from the initial build, just verified again. (3) **Check-series creation now shows a confirm/preview step before inserting**, in the shared `ScheduledPaymentsModal.tsx` (so it applies everywhere series are created — `/checks`, `/supplier-tracking`, `/expenses`, not just the new page): clicking "💾 שמור" in series mode no longer inserts immediately — it computes the would-be rows via a newly-extracted pure `buildSeriesRows(n, amount)` (same date/split/rounding-drift logic as before, just no DB call) into `seriesPreview` state, and swaps the modal's title/body/footer to a summary (count, total, per-row date+amount list) with "✏️ חזרה לעריכה" (clears `seriesPreview`, returns to the form with all fields intact) or "✓ אשר וצור" (`confirmSeriesCreate()` — inserts exactly `seriesPreview` as-is, so the preview can never drift from what's actually created, then runs the existing debt-allocation/toast/close flow). Single-check creation and editing an existing row are untouched — this is series-only, per the literal ask.
  - User separately floated (not decided, explicitly "need to think about it") merging the `/billing` tab into the supplier/customer ledger pages — flagged that `BillingClient.tsx` is a structurally different feature (recurring bills to `BillingContact`s — rent/arnona/electricity-meter, direction expense/income) from the invoice-based `supplier_debts`/`customer_ledger_debts` ledgers, so a data-model merge is probably not the right move even if a UI/nav grouping might be. Not implemented, not further explored — revisit only if the user brings it back with a firmer ask.

- **[2026-07-14] Checks pulled into a dedicated "יומן צ'קים" page (`/checks`), out of being buried in "payments":** user wanted checks (not transfers) organized in one place with month totals + filters (supplier/payee, check-number range, due-date range, status, text search), while every existing check behavior (series creation, debt allocation via `reconcileSupplierPayment`, mark-paid→auto-expense, auto-settle-at-due-date, Excel import/export) keeps working unchanged. Confirmed via AskUserQuestion: new top-level sidebar page (not a tab in Expenses/Supplier-tracking), scope = `payment_method='check'` only (transfers stay in Expenses' "📅 תשלומים" as today), and the old read-only "📅 יומן צ׳קים" calendar tab inside `SupplierTrackingClient.tsx` was **removed** and replaced with outbound links (`/checks` and `/checks?supplier=<id>`) rather than kept as a second maintained view of the same data.
  - New `components/checks/ChecksJournalClient.tsx` reuses `ScheduledPaymentsModal` for all mutation (add/edit/series) via two new optional props — `initialOpenAdd` (generalizes the existing `initialSupplierId` auto-open-add effect to also fire with no supplier preset) and `initialEditItem` (auto-opens edit for a specific row, same effect, priority over add). Excel import is *not* duplicated — a button just opens the modal's own list view where the existing importer already lives; export and print *are* duplicated (copy-adapted, scoped to the journal's filtered rows) since this codebase's established convention is copy-not-share for this kind of per-surface logic (see `reconcileCustomerLedgerPayment.ts` vs `reconcileSupplierPayment.ts`).
  - Extracted `lib/utils/markCheckPaid.ts::markScheduledPaymentPaid()` out of `ScheduledPaymentsModal.markPaid()` (the insert-expense-then-flip-is_paid logic) since the new page needed a 4th call site for materially the same operation — `ScheduledPaymentsModal` was refactored to call it too. Deliberately did **not** touch `autoMarkOverdueChecks.ts`'s separate near-duplicate implementation (different semantics: race-guarded, no user-provided pay-date/category, auto-fires at due_date) — flagged as a possible future cleanup, not done here.
  - Unlinked-to-debt checks show only a read-only ⚠ badge on `/checks` linking out to `/supplier-tracking?open=<supplierId>` — the actual retroactive-allocation picker UI was deliberately *not* rebuilt a second time there; it stays the single place that flow lives.

- **[2026-07-14] Customer payments now show as their own row in the ledger (on-screen + print), with optional receipt tracking.** User asked: (1) whether a payment creates a record in the customer ledger — it didn't, it only netted `paid` into the balance; (2) receipt tracking (was a קבלה issued, and its number) per payment; (3) a small paid/unpaid status icon per row in the *print* view (on-screen already had this via `StatusChip`). Implemented via migration `072_customer_ledger_payment_receipt.sql` adding `payment_date`/`receipt_issued`/`receipt_number` to `customer_ledger_payments` (table already existed from the 2026-07-13 ledger feature, previously write-only/never read back into the UI). `reconcileCustomerLedgerPayment.ts`'s `CustomerPaymentMeta` now requires `payment_date`/`receipt_issued`. `CustomerTrackingClient.tsx` now fetches `customer_ledger_payments` in `loadAll()` and renders each raw payment row (NOT grouped by payment event — see below) inline with debt/credit rows, sorted chronologically by `payment_date ?? check_date ?? created_at`, in both the on-screen per-month table and the printed ledger.
  - **Deliberately not grouped by payment event:** a single "אשר תשלום" click can split across several debts (multi-invoice allocation); each `customer_ledger_payments` row already carries its own per-debt amount. Rather than adding a `payment_group_id` to reassemble one combined row, each allocation row is shown as its own line (dated the same day) — simpler, and avoids a cross-date-range double-counting bug (see next point).
  - **Print running-balance correctness:** the printed ledger used to net `d.paid` in a lump sum onto the debt's own row (bug-011 fix, 2026-07-14 earlier same day) — that was already an approximation (nets at the debt's date, not the actual payment date). Now replaced: debt/credit lines and payment rows are collected as `{date, render}` events, **sorted by date first**, then rendered via `.map(ev => ev.render())` so the closured `running +=/-=` mutations execute in chronological order — do NOT go back to computing `running` before sorting, that was a bug caught during this same implementation (running must accumulate in the same order rows are displayed). Payments are only pulled in for debts already in the printed range (`debtIds.has(p.customer_ledger_debt_id)`) — payments tied to prior-period debts are already folded into `openingForReport` via `bal()`, so including them again as in-range rows would double-count.
  - Print status icon: new narrow "מצב" column, `✓` green (`is_closed`), `◐` orange (partial `paid>0`), blank (untouched open charge), `—` for credit rows; payment rows show `🧾` if a receipt was issued else `💰`.
  - Scope: customer ledger only, per the user's ask — **not** ported to `SupplierTrackingClient.tsx`'s print ledger, which per the existing 2026-07-14 bug-011 note is deliberately gross/unnetted and structured differently; don't add this without being asked.

- **[2026-07-13] Quick-fill payment amount, still routed through manual allocation:** user asked for a simple "just type the amount received/paid" input in the supplier/customer payment modals, with an option to target one specific invoice for a partial payment. Rather than committing the payment directly (which would relitigate the [[Manual over automatic for money-matching]] preference), `applyQuickAmount()` only pre-fills the *existing* `paySelectedIds`/`payAllocAmounts` state — same state the manual checkbox list edits — either spreading the typed amount oldest-open-debt-first (`payQuickTarget === 'auto'`) or dumping it all onto one chosen debt from a dropdown. The manual list stays visible below for the user to review/edit before hitting "אשר תשלום", so the auto-spread is a suggested default, never a silent decision. Reuse this pattern for any future "quick entry" request in a reconciliation flow. Also added a "מספר אסמכתא" (reference number) field for `payMethod === 'העברה'`, reusing the `check_number`/`payment_ref` columns (no schema change) — mirrors the existing check-number field.

- **[2026-07-12] Check alerts narrowed to a 5-day lookahead:** user asked that upcoming *checks* in `AlertsPanel` only appear starting 5 days before `due_date`, not the full 30-day window, so the alerts stay focused on what's actually imminent. Added `CHECK_ALERT_DAYS = 5` constant; the DB query itself still fetches everything due within 30 days (unchanged `.lte('due_date', aheadStr)`), but `setPayments()` now client-filters to `daysUntil(due_date) <= 5` when `payment_method === 'check'`. **Transfers were deliberately left at the 30-day window** — the user only mentioned checks ("צ'קים"), and transfers aren't post-dated the same way checks are. If asked to also narrow transfers, this is the filter to extend.

- **[2026-07-12] Checks auto-settle at due_date (not just at hand-over):** user confirmed a standing decision that once a scheduled check's `due_date` arrives, it should be marked paid automatically — no manual "✓ שולם" click, and it should stop showing as "באיחור" in `AlertsPanel`. This is distinct from the earlier "settled at hand-over" note below, which is about closing a *supplier debt* — this one is about the check's own `scheduled_payments.is_paid` field. Implemented `lib/utils/autoMarkOverdueChecks.ts::autoMarkOverdueChecksPaid()`: for `payment_method='check', is_paid=false, due_date<=today`, race-guarded update to `is_paid=true`+`paid_date`, then inserts the matching `expenses` row + backfills `expense_id` (same side effects as the manual `markPaid()` flow). Scoped to `payment_method='check'` only — transfers stay manual since they're user-initiated at the moment of payment, not scheduled ahead. Wired into 3 load paths: `ProfileContext.load()` (primary, admin-gated, fire-and-forget), `ScheduledPaymentsModal.fetch()` and `AlertsPanel.load()` (defensive re-checks for sessions left open across midnight, since `ProfileContext` only runs once per app mount). The manual "✓ שולם" button was deliberately left in place (not removed) — still needed for transfers and for a check cashed/settled *before* its due date.
  - **Gotcha found while investigating:** `.wolf/memory.md`'s 2026-05-27 log has an entry "DebtsClient: ... auto-expense on load for overdue payments" that sounds like this exact feature already existed — it doesn't anymore. That logic lived in the old `DebtsClient.tsx` supplier-debt UI, which was fully removed when that UI relocated to `SupplierTrackingClient.tsx` (see the 2026-07-06 entry above). Confirmed via grep that `DebtsClient.tsx` has zero `scheduled_payments`/overdue references today. Don't trust old memory.md action descriptions as evidence a feature currently exists — verify against the live file.

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- **[2026-07-05] Financial module overhaul (expenses/debts/billing):** Implemented approved plan `lively-imagining-tarjan.md`.
  - `expenses`: added `recurring_expense_id` (back-ref to `recurring_expenses` template) and `amortize_months` (nullable smallint) for manually-entered annual expenses that should be reported as prorated across N months while still showing in full on their real payment date in the normal ledger view. `ExpensesClient.tsx` summary tab has an "actual vs. prorated" toggle (`sumMode`) driving this; the main monthly tab got a category-grouped accordion (`groupedView`/`openCats`) as an alternative to the flat list.
  - Supplier checks/debts: rejected FIFO (see User Preferences). Added `supplier_debt_payments` junction table recording exactly which check/payment settled which debt, populated by `lib/debts/reconcileSupplierPayment.ts` from an **explicit** allocation list chosen in the UI (checkbox list of open debt-months per supplier, in `ScheduledPaymentsModal.tsx` and `DebtsClient.recordPayment`). This replaced the old fragile heuristic at `DebtsClient.tsx` (`due_date` month === debt month) which never actually linked checks to debts correctly. `scheduled_payments` gained `check_number` and `series_id` (for the new check-series batch-creation feature — supports equal split or "round amount + remainder on first/last check"). `DebtsClient.tsx` gained a `calendar` tab: all unpaid `scheduled_payments` across every supplier, grouped by due-month, for cash-flow forecasting.
  - Billing: found the item/entry VAT toggle (`VatToggle`, before/after) was **cosmetic only** — never affected the saved `amount`. Fixed at save time (`saveItem`/`saveEntry` in `BillingClient.tsx`): if mode is `'before'` (pre-VAT figure entered), `amount = withVat(raw)`; only applies to `type==='fixed'` (rent/arnona), never to `type==='meter'` (electricity has no VAT in this sublet arrangement, confirmed by user). Added `billing_entries.period_start/period_end` (informational, for irregular ~2-month electric meter readings) and `billing_items/entries.fixed_addon` (electric company's fixed infrastructure fee on top of kWh usage — stays on the payer's side, never charged to the sub-tenant).
- **[2026-07-06] Supplier debt detail moved to a new dedicated page** (same plan file, second round): the whole detailed supplier-debt UI that had just been built into `DebtsClient.tsx`'s suppliers tab (month-grouped invoice cards, the checks calendar, add/edit) was **relocated** into a brand-new page `app/supplier-tracking/page.tsx` + `components/supplier-tracking/SupplierTrackingClient.tsx`, reachable via a new sidebar item (`Sidebar.tsx` `NAV_ITEMS`/`ICONS`/`SECTIONS`, href `/supplier-tracking`, reuses the existing `module: 'suppliers'` permission). **`DebtsClient.tsx`'s suppliers tab is now summary-only** (supplier name + phone + final balance, a link out — no add/edit/detail, no calendar tab; `Tab` type is back to `'customers'|'suppliers'|'summary'`). If asked to touch supplier debt detail/checks/invoices UI, the code lives in `SupplierTrackingClient.tsx` now, not `DebtsClient.tsx`.
  - Also added `direction: 'charge'|'credit'` to `supplier_debts` (migration `064_supplier_debt_direction.sql`) to support supplier credit notes (returned goods/discounts) that reduce the balance, alongside regular charges. `bal()` (defined independently in `DebtsClient.tsx`, `SuppliersClient.tsx`, and `SupplierTrackingClient.tsx` — no shared util) became direction-aware: `direction==='credit' ? -amount : max(0, amount-paid)`, which automatically fixes every existing sum built on top of it. Credit rows are inserted with `paid:0, is_closed:true` immediately (never "paid" by a check).
  - New entries in `SupplierTrackingClient.tsx` are **one independent `supplier_debts` row per invoice/credit line** (own date, own direction, own `description`/notes) via bulk-insert — no more bundling multiple invoices under one shared date in the `invoices` jsonb array. Editing an existing (possibly legacy-bundled) row still does a single-row update, preserving old data untouched. The by-month view renders a real `<table>` (מספר חשבונית/תאריך/סוג/סכום) with a totals row (חיוב/זיכוי/נטו/שולם/יתרה) instead of the old `InvoiceCell` chip cards. Color convention: חיוב(charge)=red, זיכוי(credit)=green (confirmed with user — opposite of the naive red-for-credit instinct).
- **[2026-07-06] Supplier payment flow overhaul, same session:** the single-row-only payment modal in `SupplierTrackingClient.tsx` was replaced with a per-supplier multi-debt picker (checkbox list of open debt-months + "☑ שלם הכל" fill-all button, `payOpenDebts`/`payDebtsByMonth`/`paySelectedIds`/`payAllocAmounts`) feeding `reconcileSupplierPayment` with a full allocation array (the function already supported multi-row allocations — only the UI was single-row before). Added a payment-method step (מזומן/אשראי/העברה/צ'ק, reusing `ExpensesClient`'s `PaymentMethod` vocabulary): choosing צ'ק redirects to `ScheduledPaymentsModal` (which gained an `initialSupplierId` prop to pre-fill the supplier dropdown — set via `openAdd`'s default and an effect that auto-opens the add-form when passed); choosing מזומן/אשראי/העברה calls the reconciler directly **and** inserts a matching `expenses` row (same shape as the existing due-date auto-expense insert), so a direct payment shows up in the ledger exactly like a cleared check.
  - Added **retroactive check allocation**: `DebtsClient`'s "unlinked payments" warning (checks/transfers not yet tied to any debt via `supplier_debt_payments`) is now clickable per-payment — opens a small picker (same debt-checkbox pattern) that calls `reconcileSupplierPayment(..., allocatingPayment.id)` to link an *already-existing* check to debt(s) chosen now. Needed because the user is backfilling historical debts after checks for them were already issued/recorded.
  - Added `components/suppliers/QuickAddSupplierModal.tsx` (name-only-required mini form, `onCreated` callback) wired into both `SupplierTrackingClient.tsx`'s debt-entry form and `ScheduledPaymentsModal.tsx`'s supplier dropdown — replaces a `<a href="/suppliers">` link that used to navigate away and lose the open modal's state.
  - Supplier cards in `SupplierTrackingClient.tsx` are now a collapsed-by-default accordion (`openSupplierKeys`, click header to expand) — the deep-link `?open=<supplierId>` auto-expands that one card in addition to filtering search.
  - **[2026-07-08] Per-month collapse inside supplier cards:** user found that expanding a supplier showed every month's full detail at once with no way to hide individual months. Added `collapsedMonthKeys` (Set, keyed `${supplierKeyOf(sid)}::${monthKey}`) + `toggleMonthCollapsed` in `SupplierTrackingClient.tsx`, same pattern as `openSupplierKeys` but inverted default (months start expanded, not collapsed, since that matched existing behavior — only the *ability* to collapse was requested). Month header row stays visible when collapsed (month label, open/closed chip, running balance); only the invoice table + scheduled-payments block hide.
  - **[2026-07-08] Row-level edit/delete, no scroll-to-top:** same session, immediate follow-up. Previously clicking an invoice row only set `selectedId`, and the actual ✏️ ערוך / 🗑 מחק buttons lived in a "selection action bar" pinned above the by-month tab's filter row — editing a row far down a long supplier list meant scrolling all the way back up. Added inline ✏️/🗑 buttons directly in a new last `<td>` of each invoice row (`e.stopPropagation()` so they don't also toggle `selectedId`), calling the existing `openSuppModal(d)` / `deleteSuppDebt(d.id)` directly. The old top selection-action-bar (WhatsApp/pay/close-toggle) is intentionally left in place — only edit/delete got the inline shortcut since that's the specific friction the user named. If more per-row actions get requested, add them to this same `<td>` rather than reviving reliance on the top bar.
  - **[2026-07-08] Supplier opening balance + printed ledger date/month filter + running "actual balance" column:** added `suppliers.opening_balance` (migration `066_supplier_opening_balance.sql`, numeric default 0). Edited in `SuppliersClient.tsx`'s supplier edit form (that's the canonical supplier-profile page, not `SupplierTrackingClient.tsx` — see the earlier "three supplier surfaces" Do-Not-Repeat entry) alongside notes/bank fields; displayed read-only in its detail panel when non-zero.
    - **Explicitly scoped to print only for now:** asked the user whether `opening_balance` should also feed the on-screen "יתרה כוללת" totals/open-closed filtering in `SupplierTrackingClient.tsx` — user said no, print-only for now, "אולי בהמשך גם בהכל". **Do not** fold `opening_balance` into `group.totalBal`/`openSuppTotal`/month `carryOver` on-screen without asking again first; those still reflect only `supplier_debts` rows, unrelated to this field.
    - Print-choice modal (`showPrintChoice`) for the "כרטסת ספק" option gained a 3-way mode toggle — `printRangeMode`: `'all' | 'months' | 'range'`. `'months'` shows a checkbox chip list of that supplier's months (from `printSupplierMonths`, derived from their `supplier_debts`); `'range'` shows two `<input type=date>` (from/to). Print button disabled if `months` mode has zero checked.
    - In the `printMode === 'ledger'` render: `openingForReport = supplier.opening_balance + sum(bal(d) for all debts dated before the report's rangeStart)`, i.e. the opening balance always carries forward everything chronologically earlier than the printed period, even in `'months'` mode with non-contiguous month gaps (a debt in a skipped month is simply omitted from both the opening carry-in and the printed rows — acceptable edge case, not solved further per user's exact ask of "בדיוק" which months).
    - Added a 5th "יתרה בפועל" (actual/running balance) column: seeded at `openingForReport`, then accumulates `±item.amount` per invoice/credit row in chronological order — deliberately **not** netted against `paid` (gross running ledger, matching how the user described it: "היה חוב 2000 ונוסף אלף אז 3000"). An "יתרת פתיחה" row is injected first when `openingForReport !== 0 || rangeStart is set`.
    - **User's explicit visual ask:** in the printed table, credit (זיכוי) rows must have their סוג + סכום cells `text-align: left` (not the print sheet's global right-align) so a credit stands out at a glance — implemented as an inline style override on those two `<td>`s only, keyed off `d.direction === 'credit'`.
  - **[2026-07-08] Gotcha: an unapplied migration masquerades as a UI bug.** `SupplierTrackingClient.tsx`'s `loadAll()` explicitly lists `opening_balance` in its suppliers `select(...)`. If a migration adding a column (e.g. `066_supplier_opening_balance.sql`) is committed to the repo but not yet run against the live Supabase DB, that select errors, `suppRes.data` is null, and `if (suppRes.data) setSuppliers(...)` silently no-ops — leaving `suppliers` empty for the whole page load. Every supplier then renders as "ללא ספק" even though `supplier_debts.supplier_id` is correct, because `SuppliersClient.tsx` (`select('*')`) and `DebtsClient.tsx` (no `opening_balance` in its column list) don't hit the same error and keep showing supplier names fine — which is confusing since it looks like the bug is isolated to one page's join logic rather than a missing migration. **Do-Not-Repeat:** any `supabase.from(...).select(...)` with an explicit column list should have its error branch checked/surfaced (toast or console), not just `if (data) setX(data)` — otherwise a schema-migration gap silently degrades into wrong-looking data instead of a visible error. Added an error toast for this specific query as a stopgap; the actual fix is applying the migration to the live DB, which this session has no credentials to do.
  - **[2026-07-08] Per-month collapse default flipped to collapsed:** user's original ask (same day, see above) was answered with months starting *expanded*; user came back and explicitly wants collapsed-by-default instead. State renamed `collapsedMonthKeys` → `expandedMonthKeys` (membership now means "user opened this one"), so default (empty set) = everything collapsed. If asked again to change this, this is the toggle to flip.
  - Added Excel **import** for supplier debts/credits (`importExcel` in `SupplierTrackingClient.tsx`, matches the existing export column layout: ספק/מספר/סוג/סכום/שולם/תאריך/סטטוס/תיאור; matches supplier by exact name, defaults unmatched to no-supplier). Checks already had import via `ScheduledPaymentsModal`.
  - Added **styled print**: a choice modal (כרטסת ספק — per-supplier full ledger + balance totals, or יומן צ'קים — all upcoming checks) renders into a hidden `#print-area` div shown only via `@media print` (classic `visibility:hidden` on `body *` / `visibility:visible` on the print area), triggered by `window.print()` in a `useEffect` keyed on `printMode`, reset via the `afterprint` event. No new PDF/print library needed.
  - `DebtsClient.tsx`'s supplier summary tab switched from a single-column card stack to a responsive grid: `display:'grid', gridTemplateColumns:'repeat(auto-fill, minmax(260px, 1fr))'` — auto-fill naturally yields ~3 columns on medium-width content areas and ~4 on wide screens without explicit breakpoints (user asked for "at least 3 per row, 4 on wide screens").
- **[2026-07-07] Plan `vivid-launching-squid.md` — "ignore check" + "allocate to a closed debt":** triggered by a real incident (user had to delete-and-recreate a *closed* supplier debt just to be able to retroactively link a check to it, since the allocator only listed open debts). Added `scheduled_payments.allocation_ignored` (migration `065_scheduled_payments_ignore_allocation.sql`) — a per-check "✕ התעלם" dismiss button on the unlinked-payments warning in `SupplierTrackingClient.tsx` for checks intentionally not tied to any debt (e.g. an advance). Also added `allocShowClosed` toggle ("הצג גם חובות סגורים") in the retroactive-allocation picker so `allocOpenDebts` can include closed debts on demand (a "✓ סגור" tag marks them) — `reconcileSupplierPayment`/`supplier_debt_payments` already had no `is_closed` restriction, the gap was UI-only. Deliberately did NOT extend this to `ScheduledPaymentsModal.tsx`'s new-check flow — writing a brand-new check against an already-closed debt isn't a real business scenario, only *retroactively linking an existing one* is.
  - `DebtsClient.tsx`'s suppliers-tab summary was also converted from a `<table>` to a card list (avatar circle + name/phone + balance badge + "מעקב מפורט →" link), matching `SuppliersClient.tsx`'s existing card style, per explicit user preference against table/row layouts for supplier lists.
- **[2026-07-13] Checks-modal bug trio, all in `ScheduledPaymentsModal.tsx`, same root shape ("state doesn't cross a boundary"):** (1) series due-dates shifted a day early — `new Date(fDue+'T00:00:00')` parses as local time but `.toISOString().slice(0,10)` reads it back as UTC, which in Israel (UTC+2/+3) is the previous calendar day; fixed with a local-only `toLocalISODate()` helper, never use `.toISOString()` for a date that must stay in local-day terms. (2) debt selection made in `SupplierTrackingClient`'s payment picker (`paySelectedIds`/`payAllocAmounts`) was silently discarded when redirecting into the checks modal for a check payment — only `initialSupplierId` crossed that boundary; fixed by adding `initialSelectedDebtIds`/`initialDebtAllocAmounts` props consumed once via a `pendingInitialAllocRef`. (3) modal "reopened" after a successful save/edit — the list `<Modal>` and the form `<Modal>` are gated by the same parent `open` prop, and save handlers only ever reset the local `formOpen` flag, never called the parent's `onClose()`, so the list modal reappeared right after saving; fixed by calling `onClose()` in all three save-success branches (deliberate full-close, no "stay on list" behavior). **Do-Not-Repeat:** any component with a parent-controlled `open` prop + local sub-modal state needs its "done" paths to call the parent's `onClose`, not just flip the local flag — otherwise the parent's other modal (or empty state) flashes back.
- **[2026-07-13] Customer ledger (כרטסת לקוחות) built as a parallel feature, NOT a migration of the old ad-hoc `customer_debts` table.** User was explicit: not every customer who owes money becomes a "ledger customer" — the ledger is for a curated set of credit (הקפה) customers, added deliberately via a new `/customers` management page. New tables: `customers` (master, migration 067), `customer_ledger_debts` (mirrors `supplier_debts`, direction-aware — but inverted meaning: `charge` = customer owes the business, migration 068), `customer_ledger_payments` (mirrors `supplier_debt_payments`, migration 069), plus `income.customer_id`/`payment_method`/`payment_ref` columns (migration 070, mirrors what `expenses` already had) so a customer payment can be recorded as an income row. `lib/debts/reconcileCustomerLedgerPayment.ts` is a copy-not-share of `reconcileSupplierPayment.ts` pointed at the new tables (matches this codebase's established convention — `bal()` itself is duplicated per-component already). **Explicitly no incoming-check-series/calendar system** — user confirmed simple payment recording only (מזומן/אשראי/העברה/צ'ק-בודד with inline optional check number+date), so `CustomerTrackingClient.tsx` (the `/customer-tracking` page, structural mirror of `SupplierTrackingClient.tsx`) has no `ScheduledPaymentsModal` equivalent, no retroactive-allocation UI, no checks calendar tab/print. New nav items use a fresh `'customers'` module key (not reusing `'suppliers'`) since it's a distinct permission surface. `DebtsClient.tsx`'s existing ad-hoc customers tab was left fully intact — the new ledger surfaces as an additional card-grid section below it (not merged) plus a distinct 4th summary stat card, so the two customer-debt systems stay visually and numerically separable while still both feeding `netOwed`.

- **[2026-07-14] Suppliers and customers each collapsed from 2 pages to 1 (2 sub-tabs), and `/debts` narrowed to "occasional debtors" only — approved plan `sprightly-swinging-hummingbird.md`.** User's framing: too much nav fragmentation (`/suppliers` profile page + `/supplier-tracking` debt page; `/customers` profile page + `/customer-tracking` debt page), and wanted a real code-level merge (not just a tab-wrapper shim) despite the extra risk, on the condition that each phase got typechecked + browser-verified before moving on. Confirmed via AskUserQuestion: (1) deep merge over shell-wrapping, (2) new "העברות לאימות" tab in `/debts` is independent (not nested inside the customers tab), (3) `/debts`'s summary tab is removed entirely (not just narrowed), (4) call-log + bank-transfer-verification need real new tables (not just extra columns on `customer_debts`), since the ad-hoc table only ever tracked one running `paid` total with no per-payment method/history.
  - **Suppliers**: old `components/suppliers/SuppliersClient.tsx` (profile/rolodex) → moved verbatim to `SupplierDetailsTab.tsx`; old `components/supplier-tracking/SupplierTrackingClient.tsx` (debt/checks tracking, 1499 lines) → moved to `components/suppliers/SupplierTrackingTab.tsx`. Both had their own `resolveTenant`/`loadAll`/realtime-channel/`useState` data-fetch boilerplate stripped out and replaced with props (same local variable names kept, e.g. `supplierDebts`, `suppliers` — minimizes the diff across each file's large JSX body). New `components/suppliers/SuppliersClient.tsx` is a from-scratch ~180-line shell: one unified `loadAll()` (Promise.all over all 6 tables both tabs used to fetch separately), one realtime channel (union of both old channels' table subscriptions), `?tab=details|tracking&open=<id>` URL parsing (default tab = `tracking`, the day-to-day operational view), and the auto-expense-conversion side effect (used to live inside `SupplierTrackingClient`'s `loadAll`, now runs in the shell so it fires regardless of which tab is active). Duplicated `fmt`/`bal`/`waUrl` + `Supplier`/`SupplierDebt`/`ScheduledPayment`/`SupplierDebtPayment` types deduped into new `components/suppliers/shared.ts`. `/supplier-tracking` route deleted outright (not redirected — internal app, no external bookmarks to protect). Exact same pattern mirrored for customers (`CustomerDetailsTab.tsx`/`CustomerTrackingTab.tsx`/`components/customers/CustomersClient.tsx`/`components/customers/shared.ts`, `/customer-tracking` deleted). Every cross-link found via grep (`DebtsClient.tsx`, `ChecksJournalClient.tsx`, `Sidebar.tsx`) was repointed to `/suppliers?tab=tracking&open=<id>` / `/customers?tab=tracking&open=<id>`. Sidebar: both merged pages moved into the `'כספים'` nav section (matching where the old tracking pages lived), dropped from `'עסקאות'`.
  - **`/debts` (`DebtsClient.tsx`)**: `Tab` type `'customers'|'suppliers'|'summary'` → `'customers'|'transfers'`. Deleted the suppliers-tab card-grid (fully redundant with `/suppliers`'s tracking sub-tab now), the summary tab (stat cards + top-debtors lists), and the read-only `customer_ledger_debts` card block that used to sit inside the customers tab (same redundancy logic — that data is now fully owned by `/customers`'s tracking sub-tab). Customers tab renamed "💳 לקוחות" → "💳 לקוחות מזדמנים" to disambiguate from the `/customers` nav item. Added `customer_debts.due_date` (nullable date) + a due-date column (red/bold + ⚠ prefix when overdue and still open) to the table and the add/edit modal. New migration `076_customer_debt_due_date_calls_payments.sql` also adds two tables mirroring the `supplier_debt_payments`/`customer_ledger_payments` pattern: `customer_debt_calls` (`answered` boolean + optional `notes`, one row per logged call) and `customer_debt_payments` (`amount`/`payment_date`/`payment_method`/`reference`/`transfer_verified`/`verified_date` — `transfer_verified` is `null` for non-transfer methods, `false`→`true` only for `payment_method='העברה'`). New `components/debts/CallLogModal.tsx`: a small self-contained modal (answered/not-answered toggle → optional "מה נאמר" textarea → save) that also lists that debt's past calls, opened via a new "📞 שיחה" button in the row-selection action bar (shows a running count once calls exist). `recordPayment()` extended with a payment-method selector (מזומן/אשראי/העברה/צ'ק, same vocabulary as the supplier/customer-ledger flows) + optional reference/check-number field; every payment now also inserts a `customer_debt_payments` row alongside the existing `customer_debts.paid`/`is_closed` update. New "🏦 העברות לאימות" tab reads `customer_debt_payments` filtered to `payment_method='העברה'`, defaults to showing only `!transfer_verified` (toggle "הצג גם מאומתות" to include verified ones too, mirroring `SupplierTrackingTab.tsx`'s `allocShowClosed` pattern), with a per-row "✓ אומת בחשבון" button that flips `transfer_verified`+`verified_date`.
  - **Bug found and fixed during this session's verification pass, not present before the merge in a user-visible way** — see the Do-Not-Repeat entry above (`resolveTenant`/`if (!tid) return` leaving `loading` stuck forever when profile-resolution fails) — fixed in all three shells.
  - Updated `tests/forms.spec.ts`: the "לקוחות / ספקים" two-tab assertion on `/debts` now checks for "לקוחות מזדמנים" / "העברות לאימות" (the tab literally named "ספקים" no longer exists there); the `/suppliers` "add button exists" test got a `.first()` added since the default (tracking) tab now has two buttons that both loosely match `/הוסף|ספק/` (`+ הוסף חשבונית/זיכוי` and `📝 צ׳ק / סדרה לספק`) — a direct, expected consequence of merging two toolbars onto one page, not a bug.
